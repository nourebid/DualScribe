# Transcription uses personal keys: saved Gemini BYOK, or a temporary Groq key
# entered in the transcription view. No shared server API key is consumed.
# Never expose provider secrets through VITE_* variables.
APP_URL="http://localhost:3000"
